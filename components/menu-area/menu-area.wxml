<area-header
	title="{{ title }}"
	bindRightTap="handleMoreMenuTap"
/>
<scroll-view
	class="menu-list"
	style="width: {{ screenWidth }}px"
	scroll-x
>
	<block wx:for="{{ songMenu }}" wx:key="id">
		<view class="menu-item">
			<menu-item itemData="{{ item }}" />
		</view>
	</block>
</scroll-view>